import { DragDropFilesDirective } from './drag-drop-files.directive';

describe('DragDropFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropFilesDirective();
    expect(directive).toBeTruthy();
  });
});
