import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fastworld-market',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
